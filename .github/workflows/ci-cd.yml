name: CI/CD Pipeline

on:
  push:
    branches: [ feature/cicd ]
  pull_request:
    branches: [ feature/cicd ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run linter
      run: npm run lint
    
    - name: Run tests
      run: npm run test:run
    
    - name: Run tests with coverage
      run: npm run test:coverage
    
    - name: Build application
      run: npm run build
      env:
        VITE_MODEL_API_URL: ${{ secrets.MODEL_API_URL }}
        VITE_MODEL_API_TOKEN: ${{ secrets.MODEL_API_TOKEN }}

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/feature/cicd' && github.event_name == 'push'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f cicd/requirements.txt ]; then pip install -r cicd/requirements.txt; fi
    
    - name: Deploy to Domino
      run: |
        python cicd/script.py \
          "${{ secrets.DOMINO_USERNAME }}" \
          "${{ secrets.DOMINO_URL }}" \
          "${{ secrets.GITHUB_PAT }}" \
          "${{ secrets.PROJECT_NAME }}" \
          "${{ secrets.APP_NAME }}" \
          "${{ secrets.ENVIRONMENT_ID }}" \
          "${{ secrets.HARDWARE_TIER_ID }}"
      env:
        MODEL_API_URL: ${{ secrets.MODEL_API_URL }}
        MODEL_API_TOKEN: ${{ secrets.MODEL_API_TOKEN }}
